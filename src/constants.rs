// Value
pub const TIMOUT_DELAY: u64 = 10;
pub const TIMEOUT_SCREENSHOT: u64 = 60;
//pub const NUMBER_OF_THREADS: usize = 12;  // No need anymore, we use the number of CPU cores to optimize the number of thread available on the machine

// Link
pub const URL_TO_CHECK: &str = "src/resources/11websites.txt";